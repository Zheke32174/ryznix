#!/system/bin/sh
# Sourced by AxManager's install_plugin (files already extracted + perms set to 2000:2000).
ui_print "  Ryznix Nexus — consolidated control panel"
ui_print "  webroot/index.html runs shell-uid via axeron.exec()"
ui_print "  Drives @ryznix.ds/pm/vfs/rs + ryzsystemd + am/pm"
ui_print "  Also servable over Tailscale via ryznix-gateway.py"
